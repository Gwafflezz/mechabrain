# index: $scope

> [!info] Shard do índice mestre — arquivo gerado
> Escrito **só** pelo consolidador (`mechabrain consolidate`, R8.1). Índice mestre: [[index]].
> Memórias ativas (`semantic` e `procedural`) do escopo `$scope`. O mestre shardou por escopo porque passou de $threshold linhas (§9.5).

$body
