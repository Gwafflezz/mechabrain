# index

> [!info] MOC mestre — arquivo gerado
> Escrito **só** pelo consolidador (`mechabrain consolidate`, R8.1); agentes leem. Edições à mão são sobrescritas no próximo ciclo.
> Uma linha por memória ativa (`semantic` e `procedural`), agrupada por escopo. Ao passar de ~$threshold linhas o índice sharda: cada escopo ganha `indices/<escopo>.md` e este arquivo vira índice-mestre magro — uma linha por escopo, mais as memórias `$global_scope` (§9.5).
> Memórias arquivadas saem daqui mas continuam buscáveis com filtro explícito (§9.3): este é o mapa do que está vivo, não o arquivo da memória.

$body
